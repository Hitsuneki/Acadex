<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    app:cardBackgroundColor="@color/bg_surface"
    app:cardCornerRadius="12dp"
    app:strokeColor="@color/border_light"
    app:strokeWidth="1dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="12dp">

        <TextView
            android:id="@+id/txtStatus"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:textColor="@color/text_secondary"
            android:textSize="12sp"
            tools:text="1000 characters · 50 lines" />

        <TextView
            android:id="@+id/txtTruncatedNotice"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:text="File truncated. Showing first 100,000 characters."
            android:textColor="@color/star_amber"
            android:textStyle="italic"
            android:textSize="11sp"
            android:gravity="center"
            android:visibility="gone" />

        <ScrollView
            android:layout_width="match_parent"
            android:layout_height="480dp"
            android:layout_marginTop="8dp"
            android:fadeScrollbars="false"
            android:scrollbarStyle="outsideInset"
            android:scrollbars="vertical"
            android:scrollbarThumbVertical="@drawable/bg_scrollbar_thumb"
            android:scrollbarTrackVertical="@drawable/bg_scrollbar_track">

            <TextView
                android:id="@+id/viewerText"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:fontFamily="serif"
                android:paddingHorizontal="16dp"
                android:paddingVertical="12dp"
                android:lineSpacingMultiplier="1.6"
                android:textColor="@color/text_primary"
                android:textIsSelectable="true"
                android:fontFeatureSettings="kern"
                android:textSize="14sp" />
        </ScrollView>
    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
